# Deepgram API key - get one at https://deepgram.com
DEEPGRAM_API_KEY=your_deepgram_api_key_here

# Google Gemini API key - get one at https://aistudio.google.com
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Google Drive API key for browsing/importing public shared folders
GOOGLE_DRIVE_API_KEY=your_google_drive_api_key_here

# Optional: secret key to protect the app.
# If unset, the app is open for local development.
# Example generator: openssl rand -hex 32
APP_SECRET_KEY=your_secret_key_here

# Optional: host port for docker-compose to bind (container always listens on 3000).
# Change this if port 3000 is already in use on the host.
HOST_PORT=3000
